import { Meta } from "@storybook/addon-docs/blocks";
import { defaultTokens } from "@manovaspace/tokens";

<Meta title="Foundations/Breakpoints" />

# Breakpoint tokens

Responsive breakpoints are defined in `default.json` and wired into Tailwind `@theme` via `@manovaspace/ui`.

| Token | Value | Typical use |
| --- | --- | --- |
| `sm` | `{defaultTokens.breakpoint.sm}` | Large phones |
| `md` | `{defaultTokens.breakpoint.md}` | Tablets |
| `lg` | `{defaultTokens.breakpoint.lg}` | Laptops |
| `xl` | `{defaultTokens.breakpoint.xl}` | Desktops |
| `2xl` | `{defaultTokens.breakpoint['2xl']}` | Wide screens |

`@manovaspace/ui` components use **mobile-first** utilities (`sm:`, `md:`, …). Viewport preview in development: see [devtools](https://manovaspace.github.io/docs/design-system/packages/devtools/).

Full reference: [tokens docs](https://manovaspace.github.io/docs/design-system/packages/tokens/).
